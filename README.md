# RedTech

A Next.js and Tailwind CSS website for RedTech.

## Getting Started

Install dependencies:

```bash
npm.cmd install
```

Run the development server:

```bash
npm.cmd run dev
```

Open `http://localhost:3000` in your browser.
